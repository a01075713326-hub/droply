# Required
PARSE_API_KEY=
SYNC_SECRET=

# Optional: link safety check in scripts/verify.js (skipped if empty)
GOOGLE_SAFE_BROWSING_KEY=

# Optional: only used by /api/sync (lib/sync.ts), returns nothing if empty
CRYPTORANK_API_KEY=

# Optional tuning for the parser and verify script
FORCE_DETAILS=
VERIFY_MAX_PER_RUN=
VERIFY_RECHECK_DAYS=
